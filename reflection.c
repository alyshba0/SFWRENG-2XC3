
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
	puts ("AAlyshba Arshad");
	sleep (1);
	puts ("400567189");
	sleep (1);
	puts ("SFWRENG 2XC3");
	sleep (1);
	puts ("I think the most useful thing I learned in this course so far is using command line and getting used to it. The Linux environment has not only helped me in class where I have to run files through tools only accessible through the terminal but also has improved my experience with using my laptop for personal things; the command line is a clean way to organize and navigate files you can’t usually use in the regular UI.");
	sleep (1);
	puts ("There is not really anything that I have found not useful in this course do far.");
	sleep(1);
	puts ("One of the things that I think is an advantage for using Vim to code is that you have access to everything from the same place; you don’t need to open different applications to get to your files. One of the disadvantages is that because Vim is used through the command line, it is difficult to navigate and use if you don’t know, and it can seem like a lot of extra steps to compile your code and see the output. In an editor like VS Code, everything is done for you, but with Vim, it only allows you to write the actual code, not run it, so it may seem a little tedious.");
	sleep(1);
	puts ("One of the advantages of using VS Code to write your code is all the features that make it easy to code, like auto finish and specified debugging tools. These can speed up the process when it comes to writing the actual language. Also, as I said before, the fact that VS Code compiles and runs it for you with a simple button rather than through commands can make it easy to quickly check your code. One disadvantage could be that you need to download different extensions for every language you wrote in. This can be inconvenient when working with projects that include different languages, and may also take more space on your computer.");

	return EXIT_SUCCESS;
}

